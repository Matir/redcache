# Default config for redcache
listen_addr: "0.0.0.0:9999"
cache_dir: "~/.cache/redcache"

tool: {
    name: "ncat - Windows - x64"
    path: "windows/x64/ncat"
    embed: true
    source: {
        source_path: "https://gitlab.com/pentest-tools/static-binaries/-/raw/master/binaries/linux/x86_64/ncat?inline=false"
    }
}